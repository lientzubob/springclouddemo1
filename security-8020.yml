spring: 
  profiles: 
    active: 
    - dev
---
server:
  port: 8020

spring:
  profiles: dev
  application:
    name: customer
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://192.168.206.150:3306/springclouddb1
    username: root
    password: sa123456
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client: 
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/
  instance: 
    instance-id: security8020
    prefer-ip-address: true

info:
  app.name: springcloud
  company.name: lien
  build.artifactId: $project.artifactId$
  build.version: $project.version$
  
management:
  security:
    enabled: false
  endpoints: 
   web:
     exposure: 
       include: hystrix.stream
