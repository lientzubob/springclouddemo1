spring: 
  profiles: 
    active: 
    - dev
---
server:
  port: 8001

spring:
  profiles: dev
  application:
    name: customer
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://192.168.206.149:3306/springclouddb03
    username: root
    password: sa123456
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client: 
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/
  instance: 
    instance-id: customer8001
    prefer-ip-address: true

info:
  app.name: springcloud
  company.name: lien
  build.artifactId: $project.artifactId$
  build.version: $project.version$
  
management:
  security:
    enabled: false
  endpoints: 
   web:
     exposure: 
       include: hystrix.stream